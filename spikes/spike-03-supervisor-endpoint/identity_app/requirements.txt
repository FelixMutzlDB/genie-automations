# fastapi + uvicorn are pre-installed in the Databricks Apps runtime, but pinning is harmless.
fastapi
uvicorn
# psycopg (v3) is NOT pre-installed -- required or the app crashes on first Lakebase connect.
psycopg[binary]
# databricks-sdk: recent version for the Lakebase credential API + serving OpenAI client.
databricks-sdk>=0.40.0
# openai: used only as a fallback client; the SDK's get_open_ai_client() is preferred.
openai>=1.40.0
# openpyxl: the deterministic parser (parse_lib.py) needs it for xlsx (csv is stdlib).
openpyxl>=3.1.0
